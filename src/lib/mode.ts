// src/lib/mode.ts
/**
 * @description Transport-mode labels, including the plural service noun per mode.
 */

/** Plural service noun per mode. */
export const MODE_NOUN: Record<string, string> = {
  BUS: "Buses",
  TRAIN: "Trains",
  FERRY: "Ferries",
};
